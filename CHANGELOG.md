# Changelog

## Unreleased
- First packaged layout: theme at repo root, plugins under `plugins/`, `install.sh`, Hyprland part in `hypr/orbital.lua`,
  sandbox test in `scripts/test-install.sh`.
- Color picker: 18 colors (accent + glass tint), custom hex, border follows the accent, wallpaper untouched.
- `orbital.crash` + `orbital.ui`: themed crash modal and shared UI contract.
- Launcher: Recommended, per-app context menu, move-to-category, themed uninstall confirmation.
